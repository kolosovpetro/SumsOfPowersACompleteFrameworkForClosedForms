In this manuscript, we derive formulas for sums of powers by combining
centered Newton's interpolation formula for powers
\eqref{lem:central-newtons-formula-for-powers}
with hockey-stick identities
\eqref{lem:generalized-hockey-stick-identity},
and
\eqref{lem:centered-hockey-stick-identity}.
This approach can be generalized further.
In particular, while using Newton's formulas
\begin{align}
  \begin{cases}
    f(x)
    &= \sum_{k=0}^{\infty} \frac{\centralFactorial{x}{k}}{k!} \delta^{k} f(a)
    = \sum_{k=0}^{\infty} \frac{x-a}{k} \binom{x-a+\frac{k}{2}-1}{k-1} \delta^{k} f(a), \\
    f(x)
    &= \sum_{k=0}^{\infty} \frac{\fallingFactorial{x}{k}}{k!} \Delta^k f(a)
    = \sum_{k=0}^{\infty} \binom{x-a}{k} \Delta^k f(a), \\
    f(x)
    &= \sum_{k=0}^{\infty} \frac{\risingFactorial{x}{k}}{k!} \nabla^k f(a)
    = \sum_{k=0}^{\infty} \binom{x-a+k-1}{k} \nabla^k f(a).
  \end{cases}
\end{align}
one may replace
linear difference operators $\delta, \Delta, \nabla$ with their non-linear dynamic
analogs, allowing more generic approach for sums of powers.
Such non-linear difference operators arise naturally in the theory of dynamic equations on time scales,
developed by Bohner and Peterson in~\cite{Bohner2001DynamicEO}.
Thus, the discrete change $x+h$ of a function $f(x+h)$ is replaced by function $g(h)$ which
implies that generic difference operator is $D_g f = \frac{f(g(x)) - f(x)}{g(x) - x}$.
A well-known example is Jackson's $q$-difference~\cite{jackson_1909}
\begin{align*}
  D_q f(x) = \frac{f(qx) - f(x)}{qx - x},
\end{align*}
where $g(x) = qx$.
In particular, for Newton's formula in linear differences $\delta, \Delta, \nabla$, their respective
falling, rising, and central factorials are related
to binomial coefficients via
\begin{align}
  \begin{cases}
    \frac{\fallingFactorial{x}{n}}{n!} &= \frac{1}{n!} x(x-1)(x-2)\cdots(x-n-1) =\binom{x}{n}, \\
    \frac{\risingFactorial{x}{n}}{n!}  &= \frac{1}{n!} x(x+1)(x+2)\cdots(x+n-1)  =\binom{x+n-1}{n}, \\
    \frac{\centralFactorial{x}{n}}{n!} &= \frac{1}{n!} \left( n + \frac{k}{2} -1 \right)\left( n + \frac{k}{2} -2 \right)
    \cdots \left( n - \frac{k}{2} +1 \right)  =\frac{x}{n} \binom{x+\frac{n}{2}-1}{n-1}.
  \end{cases}
\end{align}
Motivated by this viewpoint, we can see that dynamic version of Newton's formula
is
\begin{align*}
  f(x) = \sum_{k=0}^{\infty} \frac{\generalizedFactorial{x}{w}{k}}{k!} D_g f(a)
\end{align*}
where $D_g f(a)$ is dynamic difference operator,
and $\generalizedFactorial{x}{w}{n} = \prod_{k=0}^{n-1} (x-w_k)$
is generalized factorial in N\"orlund's notation~\cite{Norlund1924}.
For instance,
\begin{align*}
  w_k = k: \quad \fallingFactorial{x}{n} = \generalizedFactorial{x}{w}{n} &= \prod_{k=0}^{n-1} (x-w_k) \\
  w_k = -k: \quad \risingFactorial{x}{n} = \generalizedFactorial{x}{w}{n} &= \prod_{k=0}^{n-1} (x-w_k) \\
  w_k = \frac{n-1}{2} + k: \quad \centralFactorial{x}{n} = \generalizedFactorial{x}{w}{n} &= \prod_{k=0}^{n-1} (x-w_k) \\
\end{align*}
% More formally, the algorithm to derive one of the formulas for sums of powers
% is
% \begin{algorithm}
%   \begin{enumerate}
%     \item Define difference operator $D$.
%     \item Derive Newton's formula in terms of $D$, for powers.
%     \item Find an identity that connects respective factorials with binomial coefficients (as per Newton's formula for powers).
%     \item Apply hockey stick identities to find closed form for sums of powers.
%     \item Apply hockey stick $r$-times to reach $r$-fold closed form for sums of powers.
%   \end{enumerate}
% \end{algorithm}
